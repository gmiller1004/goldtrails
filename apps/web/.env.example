# Copy to `.env.local` and fill in values (never commit `.env.local`).

# Mailchimp (lead capture)
MAILCHIMP_API_KEY=
MAILCHIMP_AUDIENCE_ID=
MAILCHIMP_SERVER=

# Shopify Storefront
SHOPIFY_STORE_DOMAIN=
SHOPIFY_STOREFRONT_ACCESS_TOKEN=
SHOPIFY_STOREFRONT_API_VERSION=2025-01

# SendGrid — contact form (`/api/contact`)
SENDGRID_API_KEY=
# Must be a verified sender in SendGrid
SENDGRID_FROM_EMAIL=
# Inbound destination for contact form messages (Vercel: SENDGRID_CONTACT_TO_EMAIL)
SENDGRID_CONTACT_TO_EMAIL=
# Optional legacy alias — used only if SENDGRID_CONTACT_TO_EMAIL is unset
# CONTACT_TO_EMAIL=
